length = size(funnyInput);

prunedFunnyInput = zeros(length(1), 2);

for i = 1:length(1)
    prunedFunnyInput(i, 1) = rankings(prunedFunnyIds{i}{1}, 1);
    prunedFunnyInput(i, 2) = rankings(prunedFunnyIds{i}{2}, 
end